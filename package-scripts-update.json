{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "postinstall": "prisma generate",
    "start": "next start",
    "lint": "next lint",
    "prisma:generate": "prisma generate",
    "prisma:deploy": "prisma migrate deploy && prisma db seed",
    "prisma:status": "prisma migrate status",
    "prisma:studio": "prisma studio",
    "vercel-build": "npm run prisma:generate && npm run prisma:deploy && npm run build",
    "deploy:check": "prisma migrate status && echo '✅ Migration status OK'",
    "deploy:safe": "prisma migrate deploy && prisma generate && npm run build",
    "deploy:verify": "prisma migrate status && node scripts/verify-indexes.js"
  }
}